# Fleš
--

## Setup
```bash
cd network
pip3 install -r requirements.txt
npm install
cd ../web
npm install
```
## Run
Start server:
```bash
cd web && npm run start
```
Start suggestions API:
```bash
python3 -m network.server
```
